# Session Log — 2026-07-28 (morning)

**Branch flow:** `main` → `chore/release-v4.21.0` → `main` → `chore/release-v4.21.1` → `main` → `chore/release-v4.21.2` → `main`
**Session topic:** Context rightsizing (Anthropic Claude 5 context-engineering guidance) — three releases + both consumers synced
**Duration:** ~4h

## Summary

Read Anthropic's two context-engineering articles (Sept 2025 + the 2026-07-24 "80% of Claude Code's system prompt removed for Claude 5" piece) and shipped their consequences as **v4.21.0 — context rightsizing, phase 1**: one-line version stamps (CLAUDE.md −54%), judgment-form comments rule (the old bullets were near-verbatim the article's cited obsolete example), and a config-gated `Skip rules` canonical-rule opt-out in the sync. A `/doctor` pass reclaimed ~5.5k est. tokens/session locally. The SiteLine sync then surfaced two upstream bugs the same day: the clobbered `linear-hygiene` prefix edit exposed a hardcoded `POC-N` that left the parent-ticket orphan-homing signal silently dead in Piper (**v4.21.1**, generic `[A-Z]+-N`), and Piper's blocking Semgrep rule caught a mutable `actions/checkout@v6` tag in the migration-drift CI template (**v4.21.2**, SHA-pinned). Both consumers synced and pulled to v4.21.2 — including Piper's twelve-release jump from v4.9.0, which **arms its hard security gate**.

## Commits shipped

Pipekit (`main`, 66f5ef3..):
- e95ab70 chore(release): v4.21.0 — context rightsizing, phase 1
- 035718b docs(claude-md): drop derivable Repo Structure section — doctor rider
- 7ac5db0 chore(release): v4.21.1 — de-hardcode /linear-hygiene parent-ticket matching
- 0a0e163 chore(release): v4.21.2 — pin migration-drift CI template action by SHA
- Merges #158/#159/#160; tags v4.21.0/.1/.2

Consumers:
- SiteLine #731 (v4.21.0 sync + linear-hygiene override rescue), #732 (v4.21.1 + override retirement), #733 (v4.21.2) — all merged
- Piper #535 (v4.9.0 → v4.21.1 sync; admin-merged past the Semgrep block), #536 (v4.21.2 pin follow-up) — merged, `dev` pulled

## Decisions / findings (worth remembering)

1. **The comments rule was the article's literal obsolete example.** `pipekit-discipline.md`'s "Default to no comments / never multi-paragraph docstrings" is near-verbatim the *before* in the Claude 5 guidance; the judgment-form *after* now ships in Claude Code's own system prompt. Incident-anchored rules are the guidance's explicit "keep" category — v4.16.0's anchor rule was already the right position.
2. **Enforcement thesis validated:** Anthropic deleted its prompt layer; nothing touches deterministic gates. `bin/pk` sentinels, hooks, and CI survive model generations; prose rules don't.
3. **Column-0 anchors make indentation load-bearing.** The `Skip rules` template row shipped commented out AND indented — pre-release testing caught that an unindented commented row still matches the config parser's `^\| \*\*Key\*\*` grep, and a verbatim template copy would have silently deleted two rules in every new project.
4. **Canonical `linear-hygiene` hardcoded `POC-N`** (SiteLine leakage) — the highest-confidence orphan-homing signal had never fired in Piper's `WIT-N` workspace. Surfaced only because the v4.21.0 sync clobbered SiteLine's in-place `(PIPER|POC)` fix. Fix: generic `[A-Z]+-N`, zero config. The override lived exactly one sync.
5. **Consumer gates now upstream-review canonical artifacts** — twice in one template: SiteLine review caught the v4.20.0 `--force` coupling; Piper's Semgrep (`github-actions-mutable-action-tag`, blocking) caught the unpinned `actions/checkout@v6` today. SHA-pin actions in all CI templates going forward.
6. **Verify consumer versions from disk, not from changelog mentions.** Piper was assumed on v4.20.0 (its name appears in recent changelog prose) but was actually on **v4.9.0** (last synced 2026-06-26) — the jump armed the v4.17 hard security gate, a real behavior change that needed prominent PR flagging.
7. **npm-global Claude Code self-updates** (proof: it sat on the day's latest while the brew cask lagged at 2.1.212 on the stable channel); brew casks only update via `brew upgrade`. Kept npm, removed the duplicate cask.
8. `/doctor` outcomes: auto mode is now the default permission mode; 15 stale skills + 5 plugins disabled; GitKraken/browseros MCP disabled for this project; machine-local `pipekit-migrations.md` rules copy deleted (~3.3k tokens/session — this repo has no migration system); skill listing is over the ~1% routing budget (vercel plugin is the largest contributor).
9. **External merges race in-flight fixes.** Piper #535 was admin-merged past the failing Semgrep check while the v4.21.2 fix was being cut; GitHub's branch auto-delete meant the fix push recreated an orphan branch → follow-up PR #536. Check PR state before pushing increments to a branch under review.

## Outstanding / next session

- **Rightsizing phase 2** (deferred by design): compress CLAUDE.md's three Key Skills tables (~1.2k est. tokens duplicating Skills_SOP/RUNBOOK), demand-load the Completion Claims loop, tier-aware rule delivery (guard-rail content for smaller Model Policy tiers → subagent spawn prompts), specs-as-code-references in `/light-spec`. Input: run `/doctor` in SiteLine.
- **Piper: first `pk ship` will hit the armed security gate** — `/security-gate` (or `/verify`) must PASS at HEAD first; escape is `--force-secgate` (plain `--force` no longer waives it).
- **Migration-drift CI adoption** (both consumers): copy `pipekit/templates/ci/migration-drift.yml` into `.github/workflows/`; if a copy is already active anywhere, re-apply the SHA pin manually — sync never rewrites workflow files.
- **SiteLine `/mcp` cleanup** (user action): disable the four dormant claude.ai connectors (Gmail, Drive, Linear, Supabase) from `/mcp` — exact names only visible there.
- **rs-vault** is not on Nebula — still on an older pk; sync from its machine.
- Piper config: consider optional `## Model Policy` section + the unset template keys listed in its sync changelog.

## QA / verification trail

| Round | Outcome | Notes |
|---|---|---|
| smoke ×3 | 133/133 each | v4.21.0/.1/.2 — no `bin/pk` behavior change in any |
| sync harness | pass | scratchpad consumer: baseline/dry-run/remove/idempotent/table-form/typo/README-refusal/restore/verbatim-copy-inert |
| verbatim-copy bug | caught pre-release | unindented commented row would have armed `Skip rules` on template copies |
| Piper Semgrep | fail → fixed → green | mutable action tag → v4.21.2 SHA pin → #536 all green |

## Linear updates

— (Pipekit tracks in session logs + CHANGELOG only; consumer syncs made no Linear transitions)
