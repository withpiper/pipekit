# Session Log — 2026-08-03 (afternoon)

**Branch flow:** `main` → `fix/lane-map-live-reconciliation` → `main` (merged, local branch deleted)
**Session topic:** SiteLine's live-`/lane-map` follow-up handover → shipped v4.29.1, then Piper worktree/branch cleanup
**Duration:** ~1h

## Summary

Processed SiteLine's same-day follow-up handover (`Pipekit_Handover_LaneMap_Live_2026.08.03_v1.md`, found in `SiteLine/Temp/`, not pipekit's own `temp/`) reporting their `/lane-map` went live with a curated+live reconciliation model. Answered all three of its open questions directly in `sop/Lane_Map_SOP.md`: softened "snapshot vs. live" from a binary choice to a decision with a documented hybrid option, added a `§ Reconciling live against curated` subsection (the three-case shape: uncurated-active → append+flag+countable, uncurated-done → fold silently, curated-but-gone → stale), and added the `mcp`-capability-vs-public-sharing 422 gotcha to `§ Artifact constraints`. Shipped as v4.29.1 (docs only), PR #178 merged, tagged. Then swept `/piper` for worktree/branch clutter — verified four branches were genuinely merged (via merge-commit content match, not just "remote gone," since three weren't clean git-ancestors of `dev` after a rebase-on-the-way-in) and removed 2 worktrees + deleted 4 local branches. Left three live-but-diverged Piper branches (`feature/WIT-418-feat-fx-cascade`, `feature/WIT-565-feat-db-per`, `spike/PIPER-462-session-continuity`) untouched pending human review.

## Commits shipped

- `9440d78` chore(release): v4.29.1 — snapshot vs. live wasn't a fork in the road
- `89af236` Merge pull request #178 from withpiper/fix/lane-map-live-reconciliation

## Decisions / findings (worth remembering)

1. Kept the reconciliation pattern scoped inside `Lane_Map_SOP.md` rather than spinning out a general "curation/live split" doc — SiteLine's own handover said they don't have a second live-artifact use case yet to prove genericity, so building the abstraction now would be premature.
2. Left `linear-hygiene`'s head-of-lane blockquote where it is, despite SiteLine noting it's now been independently re-derived wrong twice — that was an observation in their handover, not one of the three numbered asks, and relocating canonical ownership is a bigger call than a patch release should make unprompted.
3. `git branch --merged` / `git merge-base --is-ancestor` can both say "not merged" for a branch whose PR genuinely did merge, if the remote side rebased before merging — the local worktree branch's SHA never becomes a dev ancestor even though the content landed. Verified via matching commit messages + direct content diff (e.g. `method.config.md`'s `Lane map URL` line byte-identical between branch and `dev`) before force-deleting with `-D`. Worth remembering as the general check: don't trust ancestry alone when a branch's remote counterpart could have been rebased server-side.

## Outstanding / next session

- Piper and SiteLine haven't synced to pipekit v4.29.1 yet — `pipekit-update` picks it up whenever either project is next touched.
- Piper's three live-but-diverged branches (`feature/WIT-418-feat-fx-cascade`, `feature/WIT-565-feat-db-per`, `spike/PIPER-462-session-continuity`) weren't reviewed for abandonment — worth a look at some point.
- Board hygiene: — (pipekit-the-repo has no Linear board; findings tracked here and in `CHANGELOG.md` per its own tracking model)

## Linear updates

—
