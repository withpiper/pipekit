# Session log — 2026-05-27 10:12 — /seed-pr-with-test-data drafted Piper-side

## What landed

**Piper-side (where it belongs):**
- New skill at `.claude/skills/seed-pr-with-test-data/skill.md` — 5-phase flow (Discover → Propose → Pick target → Generate+Execute → Report)
- Companion SOP at `engineering/sop/Supabase_Fixtures.md` — three-tier seeding model (migrations / foundation seed / fixtures), gitignore + naming + idempotency rules
- `.gitignore` rule + `supabase/fixtures/.gitkeep` so the directory exists on fresh checkout but contents stay machine-local

**Pipekit-side (this repo):**
- Only the handoff doc captures the cross-repo state. No skill, no SOP — the skill is intrinsically Piper-specific (hardcoded user emails, `Q-Prod`/`P-Prod` names, sentinel UUIDs, `_environment.is_prod`).

## Why moved

Initial draft placed the skill in Pipekit's `skills/` + the convention doc in `resources/`. User correctly flagged: "These are piper specific and should not ride with pipekit." The skill references:

- `qa@piper.dev` / `ethan@withpiper.ai` (Piper-specific emails)
- `qa-piper-2026` / `piperlives!` (specific credentials)
- `Q-Prod` / `P-Prod` entity names
- Sentinel UUIDs `00000000-0000-4000-a000-000000000007` and `...000016`
- `_environment.is_prod` guard (Piper-specific table)

Generalizing all of that would require a `## Foundation Sentinels` block in `method.config.md` plus per-project mapping — overhead with no current beneficiary. Piper-specific skills are explicitly allowed per `sop/Skills_SOP.md` § "Project-Specific Skills (stay in each project)" — examples there include `/seed-data`, which is exactly this skill's shape.

If a future Pipekit consumer also adopts the two-user / two-entity foundation pattern, the right move is to extract the *pattern* (a stub skill + the generic three-tier model) into Pipekit and let projects layer their own sentinels on top. Premature today.

## Dry-run validation (Piper-side, against WIT-275)

Validates that Phase 1 (Discover) + Phase 2 (Propose) produce something plausible. Did not execute — WIT-275 has merged + the branch is gone.

**Discover output (simulated):**
- Branch: `feature/WIT-275-wp3-09-organize`
- Linear ID: WIT-275
- Migrations: `20260526130000_wit-275_organize_rpcs.sql` + `20260526140000_wit-275_duplicate_includes_line_items.sql`
- Schema surface touched: `public.sections`, `public.subsections`, `public.budget_line_items`

**Proposal that would be generated:** 1 budget on Q-Prod Demo, 3 sections (Above-the-Line / Below-the-Line / Post-Production), 6 subsections (2 per section), 12 line items (2 per subsection). Justification grounded in the RPCs needing ≥3 sections to verify `reorder_budget_structure`'s batch UPDATE and ≥2 subsections per section so duplicate-copies-line-items has something to verify. Sized to the surface area of the PR's RPCs, not a generic "seed 100 line items" payload.

The proposal is the right shape — phase 1+2 logic works.

Phase 4 (Generate + Execute) needs to be exercised live on the next budget-touching PR.

## Open / deferred

- `uuid_generate_v5` availability check: Piper has `uuid-ossp` enabled (`gen_random_uuid()` works); need to confirm v5 is in the same extension load before first real run. Fallback: `md5(payload)::uuid`.
- Branch-DB target via Supabase MCP is documented but unexercised — should live-test before relying on it.
- Foundation-seed identity hardening (`20260527120000_seed_dual_user_auth_identities.sql`) landed on Piper PR #379 earlier in the session. Login matrix works end-to-end (verified via local GoTrue smoke-test + browser confirmation through Vercel preview).
